language: python
python:
  - "2.7"
  - "3.5"

install:
  - pip install pytest pytest-cov

script:
  - env
  - py.test  --junitxml=report.xml --cov; fi
  - echo "finished successfully at $(date '+ %Y-%m-%d %H:%M:%S')"
  - uname -a

branches:
  only:
    - master
    - /feature-.*/